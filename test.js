"use strict";

function currentFacts(facts, schema) {
	// inputs: two arrays called "facts" and "schema".
	// facts = [entitiy, attribute, value, boolean about "added?"]
	// schema = [attribute, cardinality]
	// output: one array with the 'current facts' from the "facts" array.
	
}